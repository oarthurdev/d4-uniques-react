{
  "name": "d4proj-react",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.17.0",
    "@testing-library/react": "^13.4.0",
    "@testing-library/user-event": "^13.5.0",
    "axios": "^1.7.7",
    "bootstrap": "^5.3.3",
    "fs-extra": "^11.2.0",
    "react-bootstrap": "^2.10.4",
    "react-icons": "^5.3.0",
    "react-router-dom": "^6.26.2",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "react-select": "^5.8.0",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "npm run start-backend && npm run start-frontend",
    "build": "react-scripts build && rm -rf server/client/build && mv build server/client/",
    "generate-prisma": "sh prisma-generate.sh",
    "start-backend": "node server/app.js",
    "start-frontend": "node src/server.js",
    "deploy": "npm run generate-prisma && npm install && npm run build && npm run start"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
